# Plan: Integrate ship agent into kilo.jsonc and validate.mjs

## Context

The ship agent files exist (`.kilo/agents/ship.md`, `.kilo/commands/ship.md`) but are empty shells. AGENTS.md and README.md already reference ship in the workflow, agent table, and directory tree. `kilo.jsonc` has no ship entry in the `agent` block. `validate.mjs` still expects 6 agents (pre-ship state) and will fail on ship-related checks.

The project is pivoting to a **solo developer + AI** focus — ship replaces PR-based merge with commit-and-push workflow gating.

## Approach

Create the two empty shell files with proper content following existing agent/command patterns, add ship to `kilo.jsonc`, and update `validate.mjs` to recognize ship as the 7th agent. Minimal diff — AGENTS.md and README.md already reference ship correctly.

## Implementation Steps

### Step 1: Create ship agent prompt (`model: Pro`)

- Write `.kilo/agents/ship.md` following the flash-patch pattern: YAML frontmatter with `description`, `mode: subagent`, `steps`, `color`, then body with identity statement, verification gate, and anti-hallucination language.
- Expected: valid frontmatter, identity "You are Ship", explicit verification criteria (diff review, checks, commit message, push readiness)

### Step 2: Create ship command (`model: Flash`)

- Write `.kilo/commands/ship.md` following the debug.md pattern: frontmatter with `description` and `agent: ship`, brief body.
- Expected: `parseYamlFrontmatter` returns valid object, `agent` field is `"ship"`

### Step 3: Add ship to kilo.jsonc (`model: Flash`)

- Add `"ship": { "model": "deepseek/deepseek-v4-flash" }` to the `agent` object, placed after `flash-debug` and before `compaction`.
- Expected: `checkJsonConfig()` passes for `agent.ship has model`

### Step 4: Update validate.mjs for 7 agents (`model: Pro`)

- `checkReadmeDirectoryTree()` line 461: add `'ship.md'` to the `expected` array.
- `checkReadmeAgentCount()` line 405: change regex from `"Six focused agents"` to `"Seven focused agents"`.
- `checkAgentsMdTable()` line 321: add `|| name === 'ship'` to the subagent mode check.
- `checkAgentPromptQuality()` line 528: add `'ship'` to `verificationAgents` array.
- Expected: 0 failures when running `node tests/validate.mjs`

### Step 5: Verify (`model: Flash`)

- Run `node tests/validate.mjs` — all checks pass, 0 failures.
- Expected: ship agent appears in all relevant checks (override files, cross-reference, AGENTS.md table, command linkage, README tree, quality metrics)

## Risks / Edge Cases

- **Empty ship files**: Write content that matches the existing agent conventions exactly — same YAML frontmatter fields, same prompt structure.
- **AGENTS.md table**: Already has the ship row. `checkAgentsMdTable()` will validate model and mode against kilo.jsonc. Must match.
- **README agent count**: README already says "Seven focused agents" — validate.mjs must be updated to match.
- **README directory tree**: Already includes `ship.md`. validate.mjs expected list must include it.
- **No breaking changes**: All existing 104 checks must continue to pass. Ship adds ~6 new checks (1 agent override file = 4 checks, 1 quality check, 1 cross-reference, 1 table entry, 1 command link, 1 tree entry).
